(module info (lib "infotab.ss" "setup")
  (define name "Actinic")
  (define blurb
    (list "HTTP Client."
	  "Actinic provides quick and comfortable routines to access webservers."))
  (define primary-file "actinic.ss")
  (define doc.txt "doc.txt")
  (define categories '(net))
  (define version "0.01"))